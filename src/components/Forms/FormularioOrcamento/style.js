import styled, { keyframes } from 'styled-components';

export { 
  SectionHeader, 
  FormContainer, 
  FormTitle, 
  Form, 
  FormRow, 
  FormGroup, 
  Label 
} from '../FormularioOrdemDeServico/style';

// Caso queira customizar algo só para orçamento, adicione abaixo:
// export const CustomOrcamentoStyle = styled.div` ... `;
